{
  // Settings in this file are only applied by the integration testrunner.
  // They take precedent over all settings except those which are 
  // explicitly patched. The main purpose of this file is to provide skills
  // with a test environment, e.g., so they can use a different database for
  // tests vs. production.   
  "test": true,
  "tts": {
    "module": "dummy"
  }
}
